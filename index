#! /usr/bin/perl
use lib qq($ENV{DOCUMENT_ROOT}/.include);
use Controller;
use View;









my $answer;

$answer = Controller->handle;
$answer = View->wrap($answer);

Controller->respond($answer);
















__END__
